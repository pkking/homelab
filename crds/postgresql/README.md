This directory contains the Custom Resource Definitions (CRDs) for the homelab cloud native project.

directory structure:
- `postgresql/`: Contains the CRD for managing PostgreSQL instances in the Kubernetes cluster.